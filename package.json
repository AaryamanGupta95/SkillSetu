{
  "name": "skillsetu-root",
  "version": "1.0.0",
  "description": "Root for SkillSetu project",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev:all": "npx concurrently \"npm run server\" \"npm run client\"",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
